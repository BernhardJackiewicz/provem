__pycache__/
*.py[cod]
.pytest_cache/
.cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.env
.venv/
.venv-bench/
venv/
.idea/
.vscode/
dist/
build/
*.egg-info/
*.memory.jsonl
memory/*.jsonl
data/*
!data/README.md
transcripts/*
!transcripts/README.md
*.transcript-report.json

# Scratch outputs from fresh runs (never tracked)
docs/runs/local/
docs/runs/*.err

# LoCoMo per-QA diagnostic dumps (6-7 MB each, reproducible from the harness;
# canonical numbers live in docs/locomo_results.md)
docs/runs/locomo_*.json

# NOTE: the frozen run artifacts (docs/runs/*.jsonl, docs/runs/iter/,
# docs/runs/caches/) ARE tracked (they back the EUR-0 replay in
# scripts/verify_repro.sh; vector_cache.jsonl goes through Git LFS
# (see .gitattributes). docs/runs/manifest.json pins their hashes.

# Internal go-to-market notes (never published)
docs/growth/
