# Local Transcript Data

This directory is reserved for local transcript datasets and memory snapshots.
Do not commit real transcripts, phone numbers, emails, names, addresses,
customer records, candidate records or other PII.

Use `tests/fixtures/transcripts/` for fake test fixtures only.
